import { StyleSheet } from "react-native";

export const style = StyleSheet.create({
    container: {},
    text: {
        color: '#fff',
        position: 'absolute',
        top: 0,
        left: 0,
        marginLeft: 5
    }
})